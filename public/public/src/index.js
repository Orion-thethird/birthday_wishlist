import React from 'react';
import ReactDOM from 'react-dom/client';
import BirthdayWishlist from '../birthday-wishlist';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <BirthdayWishlist />
  </React.StrictMode>
);
